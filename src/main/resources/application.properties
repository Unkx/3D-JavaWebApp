# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/3D-JavaApp
spring.datasource.username=postgres
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT  (zmień secret przed wdrożeniem na produkcję)
app.jwt.secret=dGhpcy1pcy1hLXZlcnktc2VjcmV0LWtleS10aGF0LWlzLWxvbmctZW5vdWdo
app.jwt.expiration=604800000

# File Upload Configuration
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Include the exception reason in error responses so the frontend can show it
server.error.include-message=always

# Default administrator account (seeded on startup if missing)
app.admin.email=admin@druk3d.pl
app.admin.password=admin123
